theme = "congo"
baseURL = "https://wasatyogrodnik.github.io/"
defaultContentLanguage = "pl"
languageCode = "pl-PL"

[outputs]
home = ["HTML", "RSS"]

[markup]
  [markup.goldmark]
    [markup.goldmark.renderer]
      unsafe = true